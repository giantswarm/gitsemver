version: 2.1
orbs:
  architect: giantswarm/architect@6.15.0

workflows:
  build:
    jobs:
      - architect/go-build:
          binary: gitrepo-version
          architectures: "linux/amd64,linux/arm64"
